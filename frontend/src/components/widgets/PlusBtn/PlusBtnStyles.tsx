import { css } from "emotion";

const Styles = {
    plusBtn: css`
        transition: all .25s;
        padding: 10px;

        &:hover {
            cursor: pointer;
            background: #DCDCDC;
        }
    `
};

export default Styles;